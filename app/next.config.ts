import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Static export keeps demo hosting trivial (Vercel / IPFS).
  // Revisit if we add server actions for reminders.
};

export default nextConfig;
